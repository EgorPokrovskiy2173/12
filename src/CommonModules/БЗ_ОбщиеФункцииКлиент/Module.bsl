&НаКлиенте
Функция СоздатьСсылкуБазыЗнаний(Идентификатор, ЭтоКатегория = Ложь) Экспорт
	
	СтрокаКатегории = "";
	Если Идентификатор <> "home" Тогда
		СтрокаКатегории = ?(ЭтоКатегория, "parent=", "category=") + Идентификатор + "&";	
	КонецЕсли;
	
	НавигационнаяСсылка = "kb://api/create?" + СтрокаКатегории + "name=" + ?(ЭтоКатегория, "category", "article");
	
	Возврат НавигационнаяСсылка;
	
КонецФункции // СоздатьСсылкуБазыЗнаний()