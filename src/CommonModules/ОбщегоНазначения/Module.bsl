
&Вместо("ЭтоСсылка")
Функция БЗ_ЭтоСсылка(ПроверяемыйТип)
	Если ПроверяемыйТип = Тип("СправочникСсылка.БЗ_СтатьиБазыЗнанийПрисоединенныеФайлы") Тогда
		Возврат Истина;
	Иначе
		Результат = ПродолжитьВызов(ПроверяемыйТип);
		Возврат Результат;
	КонецЕсли;
КонецФункции
